# 🔍 Проверка базы данных и cron-задач

Если приложение говорит, что данных нет, выполните следующие шаги:

## 🚀 Быстрая проверка

### 1. Проверьте базу данных

Запустите скрипт проверки:

```bash
npm run check-db
```

Скрипт покажет:
- ✅ Есть ли записи в базе данных
- ⏰ Когда было последнее обновление
- ⚠️  Есть ли проблемы с подключением

### 2. Проверьте работу endpoint обновления

**Для локального тестирования:**
```bash
# Запустите приложение
npm run dev

# В другом терминале вызовите обновление
npm run test-update
```

**Для production (Vercel):**
```bash
npm run test-update https://ваш-домен.vercel.app
```

Или через браузер/curl:
```bash
curl "https://ваш-домен.vercel.app/api/tide/update?secret=ВАШ_СЕКРЕТ"
```

### 3. Проверьте cron-задачи в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Cron Jobs**
4. Проверьте:
   - ✅ Есть ли 4 задачи (00:00, 06:00, 12:00, 18:00 UTC)
   - ✅ Когда последний раз выполнялись
   - ✅ Есть ли ошибки в логах

## ❓ Частые проблемы

### В базе нет записей

**Причины:**
- Cron-задачи еще не отработали (подождите до 00:00, 06:00, 12:00 или 18:00 UTC)
- Cron-задачи не настроены в Vercel
- Endpoint возвращает ошибку

**Решение:**
1. Проверьте cron-задачи в Vercel Dashboard
2. Вызовите endpoint вручную: `npm run test-update`
3. Проверьте логи в Vercel Dashboard → Functions → `/api/tide/update`

### Данные устарели (больше 6 часов)

**Решение:**
1. Вызовите endpoint вручную для немедленного обновления
2. Проверьте логи выполнения cron в Vercel
3. Убедитесь, что `STORMGLASS_API_KEY` валиден

### Ошибка авторизации

**Решение:**
1. Убедитесь, что `CRON_SECRET` установлен в Vercel (Settings → Environment Variables)
2. При ручном вызове передавайте секрет: `?secret=ВАШ_СЕКРЕТ`
3. Vercel Cron автоматически передает заголовок `x-vercel-cron`, поэтому авторизация должна работать

## 📋 Чеклист

- [ ] Переменные окружения установлены в Vercel
- [ ] Таблица `tide_data` создана в Supabase
- [ ] Cron-задачи созданы в Vercel Dashboard
- [ ] Endpoint работает при ручном вызове
- [ ] Есть записи в базе данных (проверьте через `npm run check-db`)

## 📚 Подробная документация

Для более детальной диагностики см. файл `DIAGNOSTICS.md`





# Исправление проблемы чтения данных из базы данных

## Проблема
Данные успешно сохраняются в базу данных через `/api/tide/update`, но приложение не может их прочитать через `/api/tide`.

## Возможные причины

1. **RLS (Row Level Security) включен** - блокирует чтение данных
2. **Проблемы с подключением к базе данных**
3. **Неправильная структура данных**

## Решение

### ✅ Шаг 1: RLS уже отключен

RLS (Row Level Security) уже отключен для таблицы `tide_data`. Это подтверждено проверкой в Supabase Dashboard.

### ✅ Шаг 2: Локальное чтение работает

Скрипт `test-read-db.js` успешно читает данные из базы данных локально. Это означает, что:
- Подключение к базе данных работает
- Данные присутствуют в базе
- Структура данных корректна

### 🔍 Шаг 3: Проверить логи в Vercel (Production)

1. Откройте [Vercel Dashboard](https://vercel.com)
2. Выберите ваш проект
3. Перейдите в **Functions** → **Logs**
4. Вызовите `/api/tide` из приложения
5. Проверьте логи - должны появиться сообщения с префиксом `[Tide DB]` и `[Tide API]`

### Шаг 4: Проверить переменные окружения в Vercel

**Критически важно:** Убедитесь, что в Vercel установлены правильные переменные окружения:

1. Откройте [Vercel Dashboard](https://vercel.com)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Environment Variables**
4. Проверьте наличие:
   - `NEXT_PUBLIC_SUPABASE_URL` - URL вашего Supabase проекта
   - `SUPABASE_SERVICE_ROLE_KEY` - Service Role Key (не anon key!)

**Важно:** 
- Service Role Key должен быть именно **Service Role Key**, а не **anon key**
- После изменения переменных окружения нужно **передеплоить** проект

### Шаг 5: Проверить API endpoint в production

Запустите тест API endpoint на production:

```bash
API_URL=https://your-app.vercel.app node scripts/test-api-endpoint.js
```

Замените `your-app.vercel.app` на ваш реальный домен Vercel.

Этот скрипт покажет:
- Работает ли API endpoint
- Какие данные он возвращает
- Есть ли ошибки при обработке данных

## Что было исправлено в коде

1. ✅ Заменен `.single()` на `.maybeSingle()` для более безопасной обработки отсутствующих записей
2. ✅ Добавлено подробное логирование в функции чтения данных (`[Tide DB]`, `[Tide API]`)
3. ✅ Добавлено отключение RLS в миграцию (для новых установок)
4. ✅ Улучшена обработка ошибок в API endpoint
5. ✅ Добавлено логирование структуры данных перед трансформацией
6. ✅ Создан скрипт `test-api-endpoint.js` для тестирования API

## Диагностика проблемы

### Если локально все работает, но в production нет:

1. **Проверьте переменные окружения в Vercel** - это самая частая причина
2. **Проверьте логи в Vercel** после вызова `/api/tide`:
   - Должны появиться логи с префиксом `[Tide DB]` и `[Tide API]`
   - Если логов нет - возможно, endpoint не вызывается
   - Если есть ошибки - они будут видны в логах

3. **Проверьте кэш браузера/PWA**:
   - Очистите кэш приложения
   - Попробуйте открыть в режиме инкогнито
   - Используйте кнопку "Очистить кэш" в приложении

4. **Проверьте Network tab в DevTools**:
   - Откройте DevTools (F12)
   - Перейдите на вкладку Network
   - Вызовите обновление данных
   - Проверьте запрос к `/api/tide`:
     - Какой статус код?
     - Что в ответе?
     - Есть ли ошибки?

### Проверка после исправления

После выполнения всех шагов:

1. **Передеплойте проект в Vercel** (если меняли переменные окружения)
2. Обновите страницу приложения
3. Нажмите кнопку "Попробовать снова"
4. Данные должны загрузиться

Если проблема сохраняется, проверьте логи в Vercel для детальной диагностики - теперь там будет подробная информация о каждом шаге обработки данных.


# 🚀 Инструкции по развертыванию

## Локальная разработка

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Создайте `.env.local` файл:
   ```bash
   cp .env.local.example .env.local
   ```

3. Добавьте ваши API ключи в `.env.local`:
   - `STORMGLASS_API_KEY` - ключ от Stormglass API
   - `NEXT_PUBLIC_SUPABASE_URL` - URL вашего проекта Supabase
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` - анонимный ключ Supabase
   - `SUPABASE_SERVICE_ROLE_KEY` - сервисный ключ Supabase (для серверных операций)
   - `CRON_SECRET` (опционально) - секретный ключ для защиты cron endpoint

4. Создайте таблицу в Supabase:
   - Откройте SQL Editor в панели управления Supabase
   - Выполните SQL из файла `supabase/migrations/001_create_tide_data_table.sql`

5. Запустите dev сервер:
   ```bash
   npm run dev
   ```

## Production сборка

1. Соберите проект:
   ```bash
   npm run build
   ```

2. Запустите production сервер:
   ```bash
   npm start
   ```

## Развертывание на Vercel

1. Установите Vercel CLI (если еще не установлен):
   ```bash
   npm i -g vercel
   ```

2. Войдите в Vercel:
   ```bash
   vercel login
   ```

3. Разверните проект:
   ```bash
   vercel
   ```

4. Создайте таблицу в Supabase (см. раздел "Настройка базы данных" ниже)

5. Добавьте переменные окружения в настройках проекта на Vercel:
   - `STORMGLASS_API_KEY`
   - `BEACH_LATITUDE` (опционально)
   - `BEACH_LONGITUDE` (опционально)
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `CRON_SECRET` (опционально, но рекомендуется)

6. Настройте cron-задачи (см. раздел "Настройка Cron-задач" ниже)

## Развертывание на других платформах

### Netlify
- Подключите репозиторий GitHub
- Build command: `npm run build`
- Publish directory: `.next`
- Добавьте переменные окружения в настройках

### Railway
- Подключите репозиторий
- Build command: `npm run build`
- Start command: `npm start`
- Добавьте переменные окружения

## Настройка базы данных

1. Откройте SQL Editor в панели управления Supabase
2. Скопируйте содержимое файла `supabase/migrations/001_create_tide_data_table.sql`
3. Выполните SQL запрос

Или используйте Supabase CLI:
```bash
supabase db push
```

## Настройка Cron-задач

Приложение использует cron-задачи для автоматического обновления данных о приливах каждые 6 часов (00:00, 06:00, 12:00, 18:00).

📖 **Подробная инструкция:** См. файл [CRON_SETUP.md](./CRON_SETUP.md)

### Быстрая настройка на Vercel

1. **Автоматический способ (рекомендуется):**
   - Файл `vercel.json` уже настроен в проекте
   - Просто закоммитьте и запушьте его в репозиторий
   - После деплоя cron-задачи автоматически создадутся

2. **Ручной способ:**
   - Откройте Vercel Dashboard → ваш проект → Settings → Cron Jobs
   - Добавьте 4 задачи с путём `/api/tide/update` и расписаниями:
     - `0 0 * * *` (00:00)
     - `0 6 * * *` (06:00)
     - `0 12 * * *` (12:00)
     - `0 18 * * *` (18:00)

### На других платформах

Используйте внешний сервис для cron-задач:
- **cron-job.org** - бесплатный сервис
- **EasyCron** - альтернативный вариант
- **GitHub Actions** - если проект на GitHub

Подробности в [CRON_SETUP.md](./CRON_SETUP.md)

## Как это работает

1. **Cron-задачи** вызывают `/api/tide/update` каждые 6 часов
2. Endpoint запрашивает данные от Stormglass API и сохраняет их в базу данных
3. При открытии приложения `/api/tide` читает последние данные из БД
4. На основе текущего времени вычисляется актуальный прогноз прилива

Это позволяет экономить лимиты API (10 запросов в день), так как запросы к API происходят только 4 раза в сутки через cron.

## Важные замечания

- ⚠️ Не коммитьте `.env.local` в Git
- 📱 Создайте иконки для PWA (см. ICONS.md)
- 🔑 Получите API ключ на https://stormglass.io/
- 🗄️ Получите Supabase ключи в настройках проекта на https://supabase.com/dashboard
- ⏰ Настройте cron-задачи для автоматического обновления данных



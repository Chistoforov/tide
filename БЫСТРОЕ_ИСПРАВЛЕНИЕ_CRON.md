# ⚡ Быстрое исправление cron-задач

## Проблема: Cron-задачи не работают, в базе нет данных

### Решение за 3 шага:

## 1️⃣ Проверьте и задеплойте vercel.json

```bash
# Проверьте статус
git status

# Если vercel.json изменен, закоммитьте и запушьте
git add vercel.json
git commit -m "Fix cron configuration"
git push
```

После пуша подождите, пока Vercel сделает деплой (или сделайте деплой вручную).

## 2️⃣ Проверьте cron-задачи в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Ваш проект → **Settings** → **Cron Jobs**
3. Должны быть 4 задачи (00:00, 06:00, 12:00, 18:00 UTC)

**Если задач НЕТ:**
- Создайте их вручную (см. ниже) ИЛИ
- Убедитесь, что деплой завершился после пуша `vercel.json`

**Если задачи ЕСТЬ, но не выполняются:**
- Проверьте логи выполнения (кликните на задачу)
- Переходите к шагу 3

## 3️⃣ Протестируйте endpoint вручную

**Узнайте URL вашего приложения** (например: `https://tide-tracker.vercel.app`)

Затем выполните:

```bash
# Замените URL на ваш
npm run test-update https://ваш-домен.vercel.app
```

Или через curl:
```bash
curl "https://ваш-домен.vercel.app/api/tide/update"
```

**Если endpoint работает:**
- Данные должны сохраниться в базу
- Проверьте: `npm run check-db`
- Если данные появились → проблема была в cron, теперь он должен работать после деплоя

**Если endpoint НЕ работает:**
- Проверьте логи в Vercel Dashboard → Functions → `/api/tide/update`
- Проверьте переменные окружения в Vercel (Settings → Environment Variables):
  - `STORMGLASS_API_KEY`
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `SUPABASE_SERVICE_ROLE_KEY`

## 🔧 Создание cron-задач вручную (если автоматически не создались)

1. Vercel Dashboard → **Settings** → **Cron Jobs** → **Add Cron Job**

2. Создайте 4 задачи:

   **Задача 1:**
   - Path: `/api/tide/update`
   - Schedule: `0 0 * * *`

   **Задача 2:**
   - Path: `/api/tide/update`
   - Schedule: `0 6 * * *`

   **Задача 3:**
   - Path: `/api/tide/update`
   - Schedule: `0 12 * * *`

   **Задача 4:**
   - Path: `/api/tide/update`
   - Schedule: `0 18 * * *`

## ✅ После исправления

1. Вызовите endpoint вручную для немедленного обновления
2. Проверьте базу: `npm run check-db`
3. Дождитесь следующего времени cron (00:00, 06:00, 12:00, 18:00 UTC)
4. Проверьте логи выполнения в Vercel

## 📝 Важно

- Cron-задачи в Vercel работают только в **Production** окружении
- Время выполнения указано в **UTC** (не в вашем локальном времени)
- После создания/изменения cron-задач может потребоваться несколько минут для активации


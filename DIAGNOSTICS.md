# 🔍 Диагностика проблем с базой данных и cron-задачами

Этот документ поможет вам диагностировать проблемы, когда приложение говорит, что данных нет, хотя cron должен был их обновить.

## 🚀 Быстрая диагностика

### Шаг 1: Проверка базы данных

Запустите скрипт для проверки состояния базы данных:

```bash
npm run check-db
```

Или напрямую:

```bash
node scripts/check-db.js
```

**Что проверяет скрипт:**
- ✅ Наличие переменных окружения (`NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`)
- ✅ Подключение к Supabase
- ✅ Наличие записей в таблице `tide_data`
- ✅ Время последнего обновления данных
- ✅ Возраст данных (актуальны ли они)

**Что делать, если скрипт показывает ошибки:**
- Если переменные окружения не найдены → проверьте файл `.env.local` или настройки в Vercel
- Если таблица не существует → выполните миграцию в Supabase Dashboard
- Если записей нет → переходите к Шагу 2

### Шаг 2: Проверка работы endpoint обновления

Протестируйте endpoint обновления данных вручную:

**Для локального тестирования:**
```bash
npm run dev
# В другом терминале:
npm run test-update
```

**Для production (Vercel):**
```bash
npm run test-update https://your-app.vercel.app
```

Или напрямую через curl:
```bash
curl "https://your-app.vercel.app/api/tide/update?secret=YOUR_CRON_SECRET"
```

**Что проверяет скрипт:**
- ✅ Доступность endpoint
- ✅ Правильность авторизации (если используется `CRON_SECRET`)
- ✅ Успешность сохранения данных в базу

**Что делать, если endpoint возвращает ошибку:**
- Проверьте переменные окружения `STORMGLASS_API_KEY`, `BEACH_LATITUDE`, `BEACH_LONGITUDE`
- Проверьте логи в Vercel Dashboard → Functions → `/api/tide/update`
- Убедитесь, что API ключ Stormglass валиден

### Шаг 3: Проверка cron-задач в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Cron Jobs**
4. Проверьте:
   - ✅ Есть ли 4 задачи (00:00, 06:00, 12:00, 18:00 UTC)
   - ✅ Статус задач (активны ли они)
   - ✅ Историю выполнения (когда последний раз выполнялись)
   - ✅ Логи выполнения (есть ли ошибки)

**Что делать, если cron-задачи не созданы:**
- Убедитесь, что файл `vercel.json` закоммичен и запушен
- После пуша сделайте новый деплой на Vercel
- Или создайте задачи вручную через UI (см. `CRON_SETUP.md`)

**Что делать, если cron-задачи не выполняются:**
- Проверьте логи выполнения в Vercel Dashboard
- Убедитесь, что endpoint доступен (используйте Шаг 2)
- Проверьте, что все переменные окружения настроены в Vercel

## 📋 Чеклист диагностики

Используйте этот чеклист для систематической проверки:

### Переменные окружения
- [ ] `NEXT_PUBLIC_SUPABASE_URL` установлена
- [ ] `SUPABASE_SERVICE_ROLE_KEY` установлена
- [ ] `STORMGLASS_API_KEY` установлена и валидна
- [ ] `BEACH_LATITUDE` и `BEACH_LONGITUDE` установлены
- [ ] `CRON_SECRET` установлен (опционально, но рекомендуется)

### База данных
- [ ] Таблица `tide_data` создана в Supabase
- [ ] Миграция выполнена успешно
- [ ] Подключение к Supabase работает (проверьте через `npm run check-db`)

### Cron-задачи
- [ ] Файл `vercel.json` закоммичен и запушен
- [ ] Cron-задачи созданы в Vercel Dashboard
- [ ] Cron-задачи активны (не отключены)
- [ ] Есть история выполнения (хотя бы одна попытка)
- [ ] Нет ошибок в логах выполнения

### Endpoint
- [ ] Endpoint `/api/tide/update` доступен
- [ ] Endpoint возвращает успешный ответ при ручном вызове
- [ ] Данные сохраняются в базу после вызова endpoint

## 🔧 Частые проблемы и решения

### Проблема: "В базе данных нет записей"

**Возможные причины:**
1. Cron-задачи еще не отработали (подождите до следующего времени: 00:00, 06:00, 12:00, 18:00 UTC)
2. Cron-задачи не настроены в Vercel
3. Endpoint возвращает ошибку при выполнении cron
4. Проблемы с подключением к Stormglass API

**Решение:**
1. Проверьте cron-задачи в Vercel Dashboard
2. Вызовите endpoint вручную: `npm run test-update`
3. Проверьте логи в Vercel Dashboard → Functions
4. Убедитесь, что `STORMGLASS_API_KEY` валиден

### Проблема: "Данные устарели (больше 6 часов)"

**Возможные причины:**
1. Cron-задачи не выполняются
2. Endpoint возвращает ошибку
3. Проблемы с API Stormglass

**Решение:**
1. Проверьте логи выполнения cron в Vercel
2. Вызовите endpoint вручную для немедленного обновления
3. Проверьте статус API Stormglass

### Проблема: "Ошибка авторизации при вызове endpoint"

**Возможные причины:**
1. `CRON_SECRET` не установлен или неверен
2. Endpoint требует авторизацию, но она не передается

**Решение:**
1. Убедитесь, что `CRON_SECRET` установлен в Vercel
2. При ручном вызове передавайте секрет: `?secret=YOUR_SECRET`
3. Или временно отключите проверку авторизации в коде (не рекомендуется для production)

### Проблема: "Таблица tide_data не существует"

**Решение:**
1. Откройте Supabase Dashboard
2. Перейдите в SQL Editor
3. Выполните SQL из файла `supabase/migrations/001_create_tide_data_table.sql`
4. Или используйте Supabase CLI для применения миграций

## 📊 Мониторинг

### В Vercel Dashboard:
- **Settings → Cron Jobs** - список задач и история выполнения
- **Functions → /api/tide/update** - логи выполнения endpoint
- **Deployments** - логи деплоя (проверьте, что `vercel.json` применен)

### В Supabase Dashboard:
- **Table Editor → tide_data** - просмотр записей
- **SQL Editor** - выполнение запросов для проверки
- **Logs** - логи подключений и запросов

## 🆘 Если ничего не помогло

1. **Проверьте все логи:**
   - Vercel Functions logs
   - Supabase logs
   - Браузерная консоль (если проблема на фронтенде)

2. **Вызовите endpoint вручную и проверьте ответ:**
   ```bash
   curl -v "https://your-app.vercel.app/api/tide/update?secret=YOUR_SECRET"
   ```

3. **Проверьте, что все переменные окружения установлены в Vercel:**
   - Settings → Environment Variables
   - Убедитесь, что они установлены для всех окружений (Production, Preview, Development)

4. **Проверьте статус сервисов:**
   - [Vercel Status](https://www.vercel-status.com/)
   - [Supabase Status](https://status.supabase.com/)

## 📝 Полезные команды

```bash
# Проверка базы данных
npm run check-db

# Тестирование endpoint (локально)
npm run dev
npm run test-update

# Тестирование endpoint (production)
npm run test-update https://your-app.vercel.app

# Просмотр переменных окружения (локально)
cat .env.local

# Проверка подключения к Supabase через SQL
# (в Supabase Dashboard → SQL Editor)
SELECT COUNT(*) FROM tide_data;
SELECT * FROM tide_data ORDER BY fetched_at DESC LIMIT 5;
```


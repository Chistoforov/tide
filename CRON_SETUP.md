# ⏰ Подробная инструкция по настройке Cron-задач

Эта инструкция поможет вам настроить автоматическое обновление данных о приливах каждые 6 часов.

## 🎯 Что нужно сделать

Настроить автоматические запросы к API Stormglass каждые 6 часов (в 00:00, 06:00, 12:00, 18:00) для сохранения данных в базу.

---

## 📋 Вариант 1: Настройка через Vercel (Рекомендуется)

### Шаг 1: Подготовка

1. Убедитесь, что проект развернут на Vercel
2. Убедитесь, что в настройках проекта добавлена переменная окружения `CRON_SECRET` (опционально, но рекомендуется)

### Шаг 2: Автоматическая настройка (Самый простой способ)

✅ **Хорошие новости!** В проекте уже есть файл `vercel.json` с настройками cron-задач.

1. Просто закоммитьте и запушьте файл `vercel.json` в репозиторий:
   ```bash
   git add vercel.json
   git commit -m "Add cron jobs configuration"
   git push
   ```

2. После деплоя на Vercel, cron-задачи автоматически настроятся!

3. Проверьте, что cron-задачи созданы:
   - Откройте ваш проект на Vercel
   - Перейдите в раздел **Settings** → **Cron Jobs**
   - Вы должны увидеть 4 задачи:
     - `0 0 * * *` → `/api/tide/update` (00:00)
     - `0 6 * * *` → `/api/tide/update` (06:00)
     - `0 12 * * *` → `/api/tide/update` (12:00)
     - `0 18 * * *` → `/api/tide/update` (18:00)

### Шаг 3: Ручная настройка (если автоматическая не сработала)

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в раздел **Settings** (Настройки)
4. В левом меню найдите **Cron Jobs** и кликните на него
5. Нажмите кнопку **Add Cron Job** (Добавить Cron задачу)
6. Заполните форму для каждой из 4 задач:

   **Задача 1 (00:00):**
   - **Path:** `/api/tide/update`
   - **Schedule:** `0 0 * * *`
   - Нажмите **Add**

   **Задача 2 (06:00):**
   - **Path:** `/api/tide/update`
   - **Schedule:** `0 6 * * *`
   - Нажмите **Add**

   **Задача 3 (12:00):**
   - **Path:** `/api/tide/update`
   - **Schedule:** `0 12 * * *`
   - Нажмите **Add**

   **Задача 4 (18:00):**
   - **Path:** `/api/tide/update`
   - **Schedule:** `0 18 * * *`
   - Нажмите **Add**

### Шаг 4: Защита endpoint (опционально, но рекомендуется)

Если вы хотите защитить endpoint от случайных вызовов:

1. В настройках проекта Vercel добавьте переменную окружения:
   - **Name:** `CRON_SECRET`
   - **Value:** придумайте сложный секретный ключ (например: `my-super-secret-key-12345`)

2. Обновите `vercel.json`, добавив секрет в путь:
   ```json
   {
     "crons": [
       {
         "path": "/api/tide/update?secret=YOUR_SECRET_HERE",
         "schedule": "0 0 * * *"
       }
     ]
   }
   ```
   
   ⚠️ **Внимание:** Не коммитьте секретный ключ в репозиторий! Используйте переменные окружения или настройте через UI Vercel.

---

## 📋 Вариант 2: Использование внешнего сервиса (если не используете Vercel)

Если вы развернули проект на другой платформе (Netlify, Railway и т.д.), используйте внешний сервис для cron-задач.

### Использование cron-job.org (бесплатно)

1. Зарегистрируйтесь на [cron-job.org](https://cron-job.org) (бесплатно)
2. После регистрации нажмите **Create cronjob**
3. Заполните форму:
   - **Title:** `Tide Update - 00:00`
   - **Address (URL):** `https://your-domain.com/api/tide/update?secret=YOUR_SECRET`
   - **Schedule:** Выберите **Custom** и введите: `0 0 * * *`
   - Нажмите **Create cronjob**
4. Повторите для остальных времен (06:00, 12:00, 18:00):
   - Для 06:00: `0 6 * * *`
   - Для 12:00: `0 12 * * *`
   - Для 18:00: `0 18 * * *`

   Или создайте одну задачу с расписанием: `0 0,6,12,18 * * *` (будет срабатывать 4 раза в день)

### Использование EasyCron

1. Зарегистрируйтесь на [EasyCron](https://www.easycron.com)
2. Создайте новую cron-задачу
3. Укажите URL вашего endpoint
4. Настройте расписание

---

## 📋 Вариант 3: Использование GitHub Actions

Если ваш проект на GitHub, можно использовать GitHub Actions:

1. Создайте файл `.github/workflows/tide-cron.yml`:

```yaml
name: Update Tide Data

on:
  schedule:
    # Запускается в 00:00, 06:00, 12:00, 18:00 UTC
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch: # Позволяет запускать вручную

jobs:
  update-tide:
    runs-on: ubuntu-latest
    steps:
      - name: Call Update Endpoint
        run: |
          curl -X GET "https://your-domain.com/api/tide/update?secret=${{ secrets.CRON_SECRET }}"
```

2. Добавьте секрет `CRON_SECRET` в настройках репозитория:
   - Settings → Secrets and variables → Actions → New repository secret

---

## 🧪 Тестирование

После настройки cron-задач, протестируйте endpoint вручную:

1. Откройте в браузере или используйте curl:
   ```bash
   curl "https://your-domain.vercel.app/api/tide/update?secret=YOUR_SECRET"
   ```

2. Должен вернуться ответ:
   ```json
   {
     "success": true,
     "message": "Tide data saved to database successfully",
     "fetchedAt": "2024-01-01T12:00:00.000Z"
   }
   ```

3. Проверьте, что данные сохранились в Supabase:
   - Откройте Supabase Dashboard
   - Перейдите в Table Editor
   - Откройте таблицу `tide_data`
   - Должна появиться новая запись

---

## 📊 Мониторинг

### На Vercel:
- Перейдите в раздел **Settings** → **Cron Jobs**
- Вы увидите историю выполнения задач
- Можно посмотреть логи каждого запуска

### На cron-job.org:
- В разделе **Execution Log** вы увидите историю вызовов
- Можно настроить уведомления на email при ошибках

---

## ❓ Частые вопросы

**Q: Что означает расписание `0 0 * * *`?**  
A: Это cron-синтаксис. Формат: `минута час день месяц день_недели`
- `0 0 * * *` = в 00:00 каждый день
- `0 6 * * *` = в 06:00 каждый день
- `0 0,6,12,18 * * *` = в 00:00, 06:00, 12:00, 18:00 каждый день

**Q: В каком часовом поясе работает cron?**  
A: 
- Vercel Cron использует UTC
- cron-job.org позволяет выбрать часовой пояс
- GitHub Actions использует UTC

**Q: Что делать, если cron не срабатывает?**  
A:
1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что endpoint доступен (проверьте вручную)
3. Проверьте, что все переменные окружения настроены
4. Убедитесь, что таблица в Supabase создана

**Q: Можно ли изменить частоту обновлений?**  
A: Да, просто измените расписание в `vercel.json` или в настройках cron-задач. Например:
- Каждые 3 часа: `0 */3 * * *`
- Каждый час: `0 * * * *`

---

## ✅ Чеклист настройки

- [ ] Проект развернут на Vercel (или другой платформе)
- [ ] Файл `vercel.json` закоммичен и запушен (для Vercel)
- [ ] Переменная окружения `CRON_SECRET` добавлена (опционально)
- [ ] Таблица `tide_data` создана в Supabase
- [ ] Endpoint `/api/tide/update` протестирован вручную
- [ ] Cron-задачи созданы и активны
- [ ] Проверена работа cron-задач (после первого запуска)

---

## 🎉 Готово!

После настройки cron-задач, ваше приложение будет автоматически обновлять данные о приливах каждые 6 часов, экономя лимиты API Stormglass.





# 🌊 Механика подсчета приливов

## Обзор

Приложение использует **динамический пересчет** состояния прилива на основе текущего времени. Это означает, что даже если данные были сохранены в базе несколько часов назад, при каждом запросе система пересчитывает актуальное состояние, сравнивая текущее время с временными метками экстремумов.

## Что хранится в базе данных?

В базе данных хранятся **сырые данные от Stormglass API** - это массив экстремумов (пиков прилива и отлива) с временными метками на несколько дней вперед и назад.

**ВАЖНО:** Временные метки экстремумов - это **реальные данные из Stormglass API**, которые рассчитываются на основе астрономических данных (позиция Луны, Солнца, география местности). Приливы и отливы **НЕ занимают одинаковое время** - это зависит от лунных циклов, географии и других факторов.

Пример структуры данных:
```json
{
  "data": [
    {
      "time": "2024-01-15T08:30:00+00:00",
      "type": "low",    // Пик отлива
      "height": 0.5
    },
    {
      "time": "2024-01-15T14:45:00+00:00",
      "type": "high",   // Пик прилива
      "height": 3.2
    },
    {
      "time": "2024-01-15T20:20:00+00:00",
      "type": "low",
      "height": 0.6
    },
    {
      "time": "2024-01-16T02:10:00+00:00",
      "type": "high",
      "height": 3.0
    }
    // ... и так далее на несколько дней
  ]
}
```

**Обратите внимание:** Интервалы между экстремумами могут быть разными:
- От 08:30 (low) до 14:45 (high) = **6 часов 15 минут**
- От 14:45 (high) до 20:20 (low) = **5 часов 35 минут**
- От 20:20 (low) до 02:10 (high) = **5 часов 50 минут**

Это нормально! Приливы и отливы не симметричны по времени.

## Процесс пересчета при нажатии кнопки "Обновить"

### Шаг 1: Получение данных из БД
```typescript
const response = await getLatestTideDataFromDB();
// response содержит массив экстремумов с временными метками
```

### Шаг 2: Определение текущего времени
```typescript
const currentTime = new Date(); // Например: 2024-01-15T16:00:00
```

### Шаг 3: Пересчет состояния
```typescript
const tideData = transformTideData(response, currentTime);
```

## Детальная логика пересчета

### 1. Определение текущего состояния (прилив/отлив)

Функция `getCurrentTideState()` работает по следующей логике:

**Ключевая идея:** Приливы и отливы чередуются между экстремумами.

```
Временная линия:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Low (отлив)    High (прилив)   Low (отлив)    High (прилив)   Low (отлив)
    ↓               ↓               ↓               ↓               ↓
  08:30          14:45           20:20          02:10 (след)    08:00 (след)
  
  [← ОТЛИВ →]  [← ПРИЛИВ →]   [← ОТЛИВ →]   [← ПРИЛИВ →]   [← ОТЛИВ →]
```

**Алгоритм:**

1. **Находим все прошедшие экстремумы** (время < текущее время)
   ```typescript
   const pastExtremes = extremes.filter(extreme => 
     parseISO(extreme.time) < currentTime
   );
   ```

2. **Берем последний прошедший экстремум**
   ```typescript
   const lastExtreme = pastExtremes[pastExtremes.length - 1];
   ```

3. **Определяем текущее состояние:**
   - Если последний экстремум был `high` (пик прилива) → сейчас **ОТЛИВ** (вода идет вниз)
   - Если последний экстремум был `low` (пик отлива) → сейчас **ПРИЛИВ** (вода идет вверх)

**Пример:**
```
Текущее время: 16:00
Последний прошедший экстремум: 14:45 (high - пик прилива)
→ Сейчас ОТЛИВ (вода идет вниз от пика прилива)
```

### 2. Определение начала текущего состояния

Функция `getCurrentStateStart()` находит момент, когда началось текущее состояние.

**Логика:**
- Если сейчас **ПРИЛИВ** → ищем последний пик **ОТЛИВА** (low)
- Если сейчас **ОТЛИВ** → ищем последний пик **ПРИЛИВА** (high)

**Пример:**
```
Текущее время: 16:00
Текущее состояние: ОТЛИВ (определено на шаге 1)
→ Ищем последний пик прилива (high) до 16:00
→ Находим: 14:45 (high)
→ Начало текущего отлива: 14:45
```

### 3. Определение следующего экстремума

Функция `getNextExtreme()` находит ближайший будущий экстремум.

**Алгоритм:**
1. Фильтруем экстремумы, которые еще не наступили (время > текущее время)
2. Берем первый из них

**Пример:**
```
Текущее время: 16:00
Будущие экстремумы:
  - 20:20 (low)
  - 02:10 (high)
→ Следующий экстремум: 20:20 (low - пик отлива)
```

## Визуальный пример полного цикла

```
Временная линия приливов и отливов:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

08:30 (Low)    14:45 (High)    20:20 (Low)    02:10 (High)    08:00 (Low)
   ↓               ↓               ↓               ↓               ↓
[ОТЛИВ]        [ПРИЛИВ]        [ОТЛИВ]        [ПРИЛИВ]        [ОТЛИВ]
   ↑               ↑               ↑               ↑               ↑
начало          начало          начало          начало          начало
```

**Сценарий 1: Текущее время 10:00**
- Последний экстремум: 08:30 (low)
- Текущее состояние: **ПРИЛИВ** (после low идет прилив)
- Начало состояния: 08:30 (low)
- Следующий экстремум: 14:45 (high)

**Сценарий 2: Текущее время 16:00**
- Последний экстремум: 14:45 (high)
- Текущее состояние: **ОТЛИВ** (после high идет отлив)
- Начало состояния: 14:45 (high)
- Следующий экстремум: 20:20 (low)

**Сценарий 3: Текущее время 22:00**
- Последний экстремум: 20:20 (low)
- Текущее состояние: **ПРИЛИВ** (после low идет прилив)
- Начало состояния: 20:20 (low)
- Следующий экстремум: 02:10 (high)

## Откуда берутся временные метки экстремумов?

**Ключевой момент:** Временные метки экстремумов (например, "11:30" для следующего пика отлива) **НЕ вычисляются приложением** - они приходят из **Stormglass API** как готовые данные.

### Как работает Stormglass API:

1. **Астрономические расчеты:** Stormglass использует сложные астрономические модели для расчета приливов и отливов на основе:
   - Позиции Луны и Солнца
   - Географии конкретного места (широта, долгота)
   - Локальных особенностей береговой линии
   - Исторических данных

2. **Реальные временные метки:** API возвращает массив экстремумов с **точными временными метками** на несколько дней вперед и назад.

3. **Неравномерные интервалы:** Приливы и отливы **НЕ занимают одинаковое время**. Например:
   - От пика отлива до пика прилива может пройти **5-7 часов**
   - От пика прилива до пика отлива может пройти **5-7 часов**
   - Но эти интервалы **варьируются** в зависимости от лунных циклов

### Пример реальных данных:

```
Экстремумы от Stormglass API (для Carcavelos Beach):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

05:00 (high)  →  11:30 (low)  →  17:45 (high)  →  00:15 (low)
   ↓              ↓                ↓                ↓
6ч 30мин      6ч 15мин         6ч 30мин
```

**Важно:** Приложение **не вычисляет** время следующего экстремума - оно просто **находит** его в массиве данных от API, сравнивая текущее время с временными метками.

## Почему это работает?

### Преимущества динамического пересчета:

1. **Актуальность данных:** Не нужно обновлять данные каждую минуту. Достаточно иметь актуальные экстремумы на несколько дней вперед.

2. **Экономия API-запросов:** Stormglass API вызывается только раз в несколько часов через cron, а не при каждом обновлении страницы.

3. **Точность:** Состояние всегда вычисляется на основе реального текущего времени, а не времени сохранения данных.

4. **Надежность:** Временные метки экстремумов рассчитываются профессиональными астрономическими моделями, а не упрощенными формулами.

### Пример работы:

```
Данные сохранены в БД: 10:00 (15 января)
Экстремумы в БД:
  - 08:30 (low)
  - 14:45 (high)
  - 20:20 (low)
  - 02:10 (high) - следующий день

Пользователь нажимает "Обновить" в 18:00 (15 января):
→ Система берет данные из БД (сохранены в 10:00)
→ Использует текущее время: 18:00
→ Сравнивает 18:00 с экстремумами
→ Определяет: последний экстремум был 14:45 (high)
→ Результат: сейчас ОТЛИВ (начался в 14:45, следующий экстремум в 20:20)
```

## Код в действии

### Функция transformTideData()

```typescript
export function transformTideData(
  response: StormglassResponse, 
  currentTime: Date = new Date()
): TideData {
  // 1. Определяем текущее состояние (high/low)
  const currentState = getCurrentTideState(response.data, currentTime);
  
  // 2. Находим начало текущего состояния
  const currentStateStart = getCurrentStateStart(
    response.data, 
    currentTime, 
    currentState
  );
  
  // 3. Находим следующий экстремум
  const nextExtreme = getNextExtreme(response.data, currentTime);
  
  return {
    currentState,        // 'high' или 'low'
    currentStateStart,   // Когда началось текущее состояние
    nextExtreme,         // Следующий пик прилива/отлива
    lastUpdated: format(currentTime, 'dd.MM.yyyy HH:mm', { locale: ru })
  };
}
```

### Вызов в API route

```typescript
// app/api/tide/route.ts
export async function GET(request: NextRequest) {
  // Получаем сырые данные из БД
  const response = await getLatestTideDataFromDB();
  
  // ВАЖНО: Используем ТЕКУЩЕЕ время для пересчета
  const currentTime = new Date();
  const tideData = transformTideData(response, currentTime);
  
  return NextResponse.json(tideData);
}
```

## Резюме

**Механика пересчета:**
1. ✅ Данные в БД содержат временные метки экстремумов на несколько дней
2. ✅ При каждом запросе используется **текущее время** (не время сохранения)
3. ✅ Система сравнивает текущее время с экстремумами
4. ✅ Определяется текущее состояние (прилив/отлив) на основе последнего прошедшего экстремума
5. ✅ Находится начало текущего состояния и следующий экстремум
6. ✅ Результат всегда актуален, даже если данные сохранены несколько часов назад

**Ключевой момент:** Данные в БД - это "расписание" приливов и отливов. Система использует это расписание + текущее время, чтобы определить, где мы находимся прямо сейчас в цикле прилива/отлива.

---

## ⏱️ Вычисление времени и логика статусов

### Вычисление времени с начала состояния

После определения текущего состояния система вычисляет, **сколько времени прошло с начала** этого состояния:

```typescript
// В TideStatusCard.tsx
const getHoursSinceStart = (): number | null => {
  if (!currentStateStart) return null;
  const now = new Date();
  const startTime = new Date(currentStateStart.timestamp * 1000);
  const diffMs = now.getTime() - startTime.getTime();
  const diffHours = diffMs / (1000 * 60 * 60);
  return diffHours;
};
```

**Пример:**
```
Начало отлива: 14:45 (high - пик прилива)
Текущее время: 16:00
Время с начала: 16:00 - 14:45 = 1 час 15 минут = 1.25 часа
```

### Вычисление времени до следующего экстремума

Система также вычисляет, **сколько времени осталось до следующего пика**:

```typescript
// В TideStatusCard.tsx
const getTimeUntilNext = () => {
  const now = new Date();
  const nextTime = new Date(nextExtreme.timestamp * 1000);
  const diffMs = nextTime.getTime() - now.getTime();
  const diffMinutes = Math.floor(diffMs / (1000 * 60));
  // ... форматирование для отображения
};
```

**Пример:**
```
Текущее время: 16:00
Следующий экстремум: 20:20 (low - пик отлива)
Время до пика: 20:20 - 16:00 = 4 часа 20 минут
```

### Логика определения статусов

Статусы определяются на основе **времени с начала текущего состояния** и **типа состояния** (прилив/отлив):

#### Текущая логика (в TideStatusCard.tsx):

```typescript
const getStatusText = (): string => {
  const hoursSinceStart = getHoursSinceStart();
  
  if (isHigh) {
    // Для ПРИЛИВА
    if (hoursSinceStart === null || hoursSinceStart < 4) {
      return 'Пока еще можно играть';  // Прилив только начался
    } else {
      return 'Нельзя играть';           // Прилив уже давно идет
    }
  } else {
    // Для ОТЛИВА
    if (hoursSinceStart === null || hoursSinceStart < 2) {
      return 'Скоро можно играть!';     // Отлив только начался
    } else {
      return 'Можно играть!';           // Отлив уже давно идет
    }
  }
};
```

#### Визуализация логики:

```
Временная линия с статусами:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

08:30 (Low)    14:45 (High)    20:20 (Low)    02:10 (High)
   ↓               ↓               ↓               ↓
[ОТЛИВ]        [ПРИЛИВ]        [ОТЛИВ]        [ПРИЛИВ]
   │               │               │               │
   │               │               │               │
   ├─ 0-2ч:        ├─ 0-4ч:        ├─ 0-2ч:        ├─ 0-4ч:
   │  "Скоро       │  "Пока еще    │  "Скоро       │  "Пока еще
   │   можно       │   можно       │   можно       │   можно
   │   играть!"    │   играть"     │   играть!"    │   играть"
   │               │               │               │
   ├─ 2ч+:         ├─ 4ч+:         ├─ 2ч+:         ├─ 4ч+:
   │  "Можно       │  "Нельзя      │  "Можно       │  "Нельзя
   │   играть!"    │   играть"     │   играть!"    │   играть"
```

### Детальные примеры с расчетами

#### Пример 1: Отлив только начался

```
Экстремумы:
  - 14:45 (high) - пик прилива
  - 20:20 (low) - пик отлива

Текущее время: 15:30
─────────────────────────────────────
1. Текущее состояние: ОТЛИВ (после high)
2. Начало отлива: 14:45
3. Время с начала: 15:30 - 14:45 = 0.75 часа (< 2 часов)
4. Следующий экстремум: 20:20 (low)
5. Время до пика: 4 часа 50 минут

Результат:
- Статус: "Скоро можно играть!" (отлив < 2 часов)
- Цвет: оранжевый
```

#### Пример 2: Отлив в разгаре

```
Текущее время: 17:00
─────────────────────────────────────
1. Текущее состояние: ОТЛИВ
2. Начало отлива: 14:45
3. Время с начала: 17:00 - 14:45 = 2.25 часа (>= 2 часов)
4. Следующий экстремум: 20:20 (low)
5. Время до пика: 3 часа 20 минут

Результат:
- Статус: "Можно играть!" (отлив >= 2 часов)
- Цвет: желтый
```

#### Пример 3: Прилив только начался

```
Экстремумы:
  - 20:20 (low) - пик отлива
  - 02:10 (high) - пик прилива

Текущее время: 21:00
─────────────────────────────────────
1. Текущее состояние: ПРИЛИВ (после low)
2. Начало прилива: 20:20
3. Время с начала: 21:00 - 20:20 = 0.67 часа (< 4 часов)
4. Следующий экстремум: 02:10 (high)
5. Время до пика: 5 часов 10 минут

Результат:
- Статус: "Пока еще можно играть" (прилив < 4 часов)
- Цвет: оранжевый
```

#### Пример 4: Прилив в разгаре (высокая вода)

```
Текущее время: 01:00 (следующий день)
─────────────────────────────────────
1. Текущее состояние: ПРИЛИВ
2. Начало прилива: 20:20 (предыдущий день)
3. Время с начала: 4.67 часа (>= 4 часов)
4. Следующий экстремум: 02:10 (high)
5. Время до пика: 1 час 10 минут

Результат:
- Статус: "Нельзя играть" (прилив >= 4 часов)
- Цвет: синий
```

### Полная таблица статусов

| Состояние | Время с начала | Статус | Цвет | Описание |
|-----------|----------------|--------|------|----------|
| **ОТЛИВ** | < 2 часов | "Скоро можно играть!" | 🟠 Оранжевый | Отлив только начался, вода еще уходит |
| **ОТЛИВ** | >= 2 часов | "Можно играть!" | 🟡 Желтый | Отлив в разгаре, можно играть |
| **ПРИЛИВ** | < 4 часов | "Пока еще можно играть" | 🟠 Оранжевый | Прилив только начался, вода еще не очень высокая |
| **ПРИЛИВ** | >= 4 часов | "Нельзя играть" | 🔵 Синий | Прилив в разгаре, играть нельзя |

### Почему используются пороги 2 часа для отлива и 4 часа для прилива?

**Физика приливов:**
- Полный цикл прилива-отлива занимает примерно **12-13 часов**
- От пика до пика проходит около **6-6.5 часов**
- Для **отлива**: первые **2 часа** - переходный период, когда вода еще уходит
- Для **прилива**: первые **4 часа** - переходный период, когда вода еще не очень высокая

**Логика для игры:**
- **Отлив < 2ч**: Вода еще уходит, скоро можно будет играть
- **Отлив >= 2ч**: Вода достаточно ушла, можно играть
- **Прилив < 4ч**: Вода еще не очень высокая, пока можно играть
- **Прилив >= 4ч**: Вода высокая, играть нельзя

### Улучшенная логика (рекомендация)

Текущая логика использует только время с начала состояния. Можно улучшить, учитывая также **время до следующего экстремума**:

```typescript
// Улучшенная логика (пример)
const getStatusText = (): string => {
  const hoursSinceStart = getHoursSinceStart();
  const hoursUntilNext = getHoursUntilNext();
  
  if (isHigh) {
    // ПРИЛИВ
    if (hoursSinceStart < 2) {
      return 'Пока еще можно играть';
    } else if (hoursUntilNext < 2) {
      return 'Скоро нельзя будет играть';
    } else {
      return 'Высокая вода';
    }
  } else {
    // ОТЛИВ
    if (hoursSinceStart < 2) {
      return 'Скоро можно играть!';
    } else if (hoursUntilNext < 2) {
      return 'Скоро нельзя будет играть';
    } else {
      return 'Можно играть!';
    }
  }
};
```

Но текущая логика с порогом 3 часа работает хорошо для большинства случаев.

---

## 📊 Сводная таблица вычислений

| Параметр | Как вычисляется | Где используется |
|----------|----------------|------------------|
| **Текущее состояние** | Сравнение текущего времени с последним прошедшим экстремумом | Определение прилив/отлив |
| **Начало состояния** | Поиск последнего экстремума противоположного типа | Вычисление времени с начала |
| **Время с начала** | `текущее_время - начало_состояния` | Определение статуса |
| **Следующий экстремум** | Первый будущий экстремум из массива | Отображение времени до пика |
| **Время до пика** | `следующий_экстремум - текущее_время` | Информация для пользователя |
| **Статус** | `время_с_начала` + `тип_состояния` | Отображение в UI |

---

## 🎯 Резюме по времени и статусам

**Ключевые вычисления:**
1. ✅ **Время с начала состояния** = текущее время - время начала состояния
2. ✅ **Время до следующего пика** = время следующего экстремума - текущее время
3. ✅ **Статус** определяется на основе времени с начала:
   - Отлив: < 2ч или >= 2ч
   - Прилив: < 4ч или >= 4ч

**Логика статусов:**
- 🟠 **Оранжевый**: Переходный период (отлив < 2ч или прилив < 4ч)
- 🟡 **Желтый** (отлив >= 2ч): Можно играть
- 🔵 **Синий** (прилив >= 4ч): Нельзя играть

**Важно:** Все вычисления происходят на основе **текущего времени**, поэтому статусы всегда актуальны, даже если данные в БД были сохранены несколько часов назад.

